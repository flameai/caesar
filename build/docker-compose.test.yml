version: "3.4"
services:
  test:
    image: caesar
    env_file:
      - ../.env
    ports:
      - "8000:8000"
    command: ["sh", "./build/run_tests.sh"]
    depends_on:
      - postgres

  postgres:
    image: postgres:15.4
    env_file:
      - ../.env
    ports:
      - "5432:5432"