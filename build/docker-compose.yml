version: "3.4"
services:
  web:
    image: caesar
    env_file:
      - ../.env
    ports:
      - "8000:8000"
    command: ["python", "-m", "source.app"]
    depends_on:
      - postgres

  postgres:
    image: postgres:15.4
    env_file:
      - ../.env
    ports:
      - "5432:5432"